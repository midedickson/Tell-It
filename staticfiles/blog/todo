create a link from signup page to lead to login
improve the link on login page to signup
add gender to profile: male, female, rather not say.
change btn colors and user profile link to blue
Just In case
users = User.objects.all().select_related('profile')
set up the update view